//
//  OverallResultCalculator.swift
//  Symteo
//
//  Created by 박병선 on 1/24/26.
//
import Foundation

/*
struct OverallResultCalculator {

    static func calculate(
        phqScore: Int,
        gadScore: Int
    ) -> OverallResult {

        // 평균 점수 (반올림)
        let averageScore = Int(
            round(Double(phqScore + gadScore) / 2.0)
        )

        // 상태 도출
        let status = OverallResultStatus.from(
            phqScore: phqScore,
            gadScore: gadScore
        )

        return OverallResult(
            phqScore: phqScore,
            gadScore: gadScore,
            averageScore: averageScore,
            status: status
        )
    }
}
*/
